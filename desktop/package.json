{
  "name": "codex-adapter-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Codex Adapter Electron desktop app",
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix ..",
    "start": "npm run build && electron src/main.mjs",
    "dev": "npm run build && electron src/main.mjs",
    "pack": "npm run build && electron-builder --win --dir",
    "dist": "npm run build --prefix .. && electron-builder --win --publish never"
  },
  "dependencies": {
    "express": "^5.2.1",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^25.6.0",
    "electron": "^34.5.1",
    "electron-builder": "^25.1.8",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
